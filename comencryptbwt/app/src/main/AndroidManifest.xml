<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.encrypt.bwt"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- (Opcional) Permiso de Cámara si escaneas QR -->
    <!-- <uses-permission android:name="android.permission.CAMERA" /> -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Encryption">

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Para ENCRIPTAR texto seleccionado (ACTION_PROCESS_TEXT) -->
        <activity
            android:name=".EncryptProcessActivity"
            android:label="@string/action_encrypt_label"
            android:exported="true"
            tools:ignore="AppLinkUrlError">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>

        <!-- Para DESENCRIPTAR texto seleccionado (ACTION_PROCESS_TEXT) -->
        <activity
            android:name=".DecryptProcessActivity"
            android:label="@string/action_decrypt_label"
            android:exported="true"
            tools:ignore="AppLinkUrlError">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>

        <!-- ENCRIPTAR al COMPARTIR (ACTION_SEND) => handle text/* y file/* -->
        <activity
            android:name=".EncryptShareActivity"
            android:label="@string/share_encrypt_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <!-- Aceptar texto: -->
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <!-- Aceptar cualquier archivo: -->
                <data android:mimeType="*/*"/>
            </intent-filter>
        </activity>

        <!-- DESENCRIPTAR al COMPARTIR (ACTION_SEND) => handle text/* y file/* -->
        <activity
            android:name=".DecryptShareActivity"
            android:label="@string/share_decrypt_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <!-- Aceptar texto: -->
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <!-- Aceptar cualquier archivo: -->
                <data android:mimeType="*/*"/>
            </intent-filter>
        </activity>

        <!-- Import Key from QR -->
        <activity
            android:name=".ImportKeyActivity"
            android:exported="true"
            android:label="@string/import_key_from_qr"/>

        <!-- KeyManager -->
        <activity
            android:name=".KeyManagerActivity"
            android:label="@string/manage_keys_title"
            android:exported="true"/>

        <!-- QrScannerActivity -->
        <activity
            android:name=".QrScannerActivity"
            android:exported="false"/>

        <!-- NUEVO: Activity para manejo local de archivos -->
        <activity
            android:name=".FileEncryptionActivity"
            android:label="File Encrypt/Decrypt"
            android:exported="true"/>
    </application>

</manifest>
